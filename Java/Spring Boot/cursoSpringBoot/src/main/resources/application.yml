spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cncdb
    username: pacopepe
    password: 123admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #Puede tener los valores "create"
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true



